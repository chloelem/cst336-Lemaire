#include <stdio.h>
#define N 5
void inv (int tab [N]);

int main ()
{     
     int T[N];
     int i;
     for (i=0 ; i<N; i++)

	

       scanf("%d", T+i);

for (i=0 ; i<N; i++)
    printf("%d\n", T[i]);
return 0;

}